# 🍀 SproutLine

SproutLine — это мессенджер нового поколения, созданный для скорости, приватности и полного контроля. Без ограничений, без компромиссов — создан для обучения и удобного общения.

## ✨ Возможности

- **Мгновенная доставка сообщений** — минимальные задержки благодаря оптимизированному серверу
- **Обмен изображениями** — отправка и получение изображений с автоматическим сжатием
- **Аутентификация пользователей** — безопасная регистрация и вход в систему
- **Административный контроль** — управление пользователями, баны и административные команды
- **История сообщений** — автоматическая синхронизация истории для новых подключений
- **Индикаторы печати** — видите, когда пользователи печатают
- **Современный интерфейс** — интерфейс на базе CustomTkinter с поддержкой темной темы
- **Кроссплатформенность** — работает на Windows, macOS и Linux
- **Часовые пояса** — автоматическая конвертация времени для сообщений

## 📋 Требования

- Python 3.8 или выше
- Зависимости указаны в `requirements.txt`

## 🚀 Быстрый старт

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/fail4k/SproutLine.git
cd SproutLine
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

### Запуск сервера

1. Запустите сервер:
```bash
python server.py
```

2. Введите IP-адрес (по умолчанию: 127.0.0.1)
3. Введите порт (по умолчанию: 1604)

### Запуск клиента

1. Запустите клиент:
```bash
python client.py
```

2. Введите IP сервера и порт
3. Зарегистрируйте новый аккаунт или войдите с существующими данными

## 🐳 Развертывание через Docker

### Использование Docker Compose

1. Соберите и запустите контейнеры:
```bash
docker-compose up -d
```

2. Сервер будет доступен на порту 1604
3. Подключите клиенты к IP сервера

### Ручная настройка Docker

#### Сборка образа сервера:
```bash
docker build -t sproutline-server -f Dockerfile.server .
```

#### Сборка образа клиента:
```bash
docker build -t sproutline-client -f Dockerfile.client .
```

#### Запуск сервера:
```bash
docker run -d -p 1604:1604 --name sproutline-server sproutline-server
```

## 📖 Использование

### Команды сервера

Сервер предоставляет интерактивное меню для администрирования:

- **Users** — список пользователей, кик, бан или разбан
- **Admins** — добавление или удаление администраторов
- **Chat** — просмотр или сохранение истории чата
- **Exit** — корректное завершение работы сервера

### Возможности клиента

- **Отправка сообщений** — введите текст и нажмите Enter для отправки
- **Отправка изображений** — нажмите кнопку фото, чтобы прикрепить и отправить изображение
- **Смена никнейма** — обновите отображаемое имя (требует входа)
- **Смена пароля** — обновите пароль аккаунта
- **Административные команды** — используйте команды с `/` если вы администратор:
  - `/kick <имя_пользователя>` — кикнуть пользователя из чата
  - `/ban <имя_пользователя>` — забанить пользователя
  - `/unban <имя_пользователя>` — разбанить пользователя
  - `/set_admin <имя_пользователя>` — предоставить права администратора
  - `/remove_admin <имя_пользователя>` — отозвать права администратора
  - `/list_admins` — показать всех администраторов
  - `/list_banned` — показать всех забаненных пользователей

## 🏗️ Структура проекта

```
SproutLine/
├── client.py              # Клиентское приложение
├── server.py              # Серверное приложение
├── requirements.txt       # Зависимости Python
├── Dockerfile.server      # Docker конфигурация для сервера
├── Dockerfile.client      # Docker конфигурация для клиента
├── docker-compose.yml     # Docker Compose конфигурация
├── assets/                # Ресурсы приложения
│   ├── config/           # Файлы конфигурации
│   └── images/           # Изображения интерфейса
└── src/                  # Исходные файлы
    └── img/              # Пользовательские изображения
```

## 🔧 Конфигурация

Настройки сервера и клиента можно изменить в:
- `assets/config/servers.json` — настройки подключения к серверу
- `assets/config/settings.json` — настройки приложения
- `assets/config/user_data.json` — пользовательские настройки

## 🛡️ Безопасность

- Пароли хешируются с использованием SHA-256
- База данных SQLite для управления пользователями
- Система банов на основе IP
- Валидация и санитизация входных данных

## 📝 Лицензия

Этот проект предоставляется как есть для образовательных и личных целей.

## 🤝 Вклад в проект

Приветствуются любые вклады! Пожалуйста, создавайте Pull Request.

## 📧 Поддержка

По вопросам и проблемам, пожалуйста, создавайте issue на GitHub.

---

**Примечание**: Для работы приложения требуется сетевое подключение между сервером и клиентами. Для локальных сетей рекомендуется использовать такие инструменты как Radmin VPN или Hamachi.
